# Reference PDFs

These are the PDFs produced by our full-sweep run on Clemson CloudLab (r6615 server + c6420 client). Reviewers should compare their own output against these to verify shape match.

| File | Notes |
|---|---|
| `fig4a_latency.pdf` | Monitoring-query latency vs versions |
| `fig4b_throughput.pdf` | Monitoring-query throughput vs versions |
| `fig4c_payload.pdf` | Response payload vs versions |
| `fig5_put_throughput.pdf` | Key-update throughput vs users (broken y-axis) |

Absolute numbers depend on hardware. Trends and protocol ordering are the artifact-evaluation criteria.
